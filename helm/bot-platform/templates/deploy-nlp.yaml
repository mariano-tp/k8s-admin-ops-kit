apiVersion: apps/v1
kind: Deployment
metadata:
  name: bot-platform-nlp
  labels:
    app.kubernetes.io/name: {{ include "bot-platform.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  replicas: {{ .Values.replicaCount.nlp }}
  selector:
    matchLabels:
      app: bot-platform-nlp
  template:
    metadata:
      labels:
        app: bot-platform-nlp
        app.kubernetes.io/name: {{ include "bot-platform.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/managed-by: {{ .Release.Service }}
    spec:
      containers:
        - name: nlp
          image: {{ .Values.image.nlp }}
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: bot-platform-nlp-pdb
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: bot-platform-nlp
